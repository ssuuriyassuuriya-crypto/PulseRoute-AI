import { Bar, BarChart, CartesianGrid, ResponsiveContainer, Tooltip, XAxis, YAxis } from "recharts";
import { useDashboard } from "../contexts/DashboardContext";

export function AnalyticsPage() {
  const { state } = useDashboard();
  const data = Object.values(state?.analytics.roads ?? {});
  return <section><p className="text-sm text-emerald-300">LIVE PERFORMANCE</p><h1 className="mt-1 text-3xl font-semibold">Traffic analytics</h1><div className="mt-7 grid gap-5 xl:grid-cols-[1.4fr_.8fr]"><article className="h-96 rounded-2xl border border-white/10 bg-[#161b22] p-5"><h2 className="font-semibold">Vehicle count by road</h2><div className="mt-5 h-72"><ResponsiveContainer width="100%" height="100%"><BarChart data={data}><CartesianGrid stroke="#ffffff14" vertical={false} /><XAxis dataKey="road" stroke="#94a3b8" tickLine={false} axisLine={false} /><YAxis stroke="#94a3b8" tickLine={false} axisLine={false} /><Tooltip cursor={{ fill: "#ffffff0a" }} contentStyle={{ background: "#161b22", border: "1px solid #ffffff1a", borderRadius: 8 }} /><Bar dataKey="vehicle_count" fill="#00e676" radius={[6, 6, 0, 0]} /></BarChart></ResponsiveContainer></div></article><article className="rounded-2xl border border-white/10 bg-[#161b22] p-5"><h2 className="font-semibold">Road pressure</h2><div className="mt-5 space-y-4">{data.map((road) => <div key={road.road}><div className="flex justify-between text-sm"><span>{road.road}</span><span className="text-slate-400">{road.density_score}</span></div><div className="mt-2 h-2 overflow-hidden rounded-full bg-white/10"><div className="h-full rounded-full bg-sky-400" style={{ width: `${Math.min(100, road.density_score)}%` }} /></div><p className="mt-1 text-xs text-slate-500">Queue {road.queue_length_meters}m · wait {road.average_wait_seconds}s</p></div>)}</div></article></div></section>;
}
